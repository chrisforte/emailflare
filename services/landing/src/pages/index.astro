---
import Base from '../layouts/Base.astro';
import { Icon } from 'astro-icon/components';

const structuredData = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  "name": "EmailFlare",
  "url": "https://emailflare.dev",
  "description": "EmailFlare ships two open-source products: a REST email sending API on Cloudflare Email Sending (Docker or Workers), and a hosted team inbox built on Cloudflare Email Routing with D1, R2, and Durable Objects.",
  "applicationCategory": "DeveloperApplication",
  "operatingSystem": "Docker",
  "license": "https://github.com/0xdps/emailflare/blob/trunk/LICENSE",
  "codeRepository": "https://github.com/0xdps/emailflare",
  "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" }
};
---

<Base
  title="EmailFlare — Email API and Inbox for Cloudflare"
  description="EmailFlare ships two open-source products: a REST email sending API on Cloudflare Email Sending (Docker or Workers), and a hosted team inbox built on Cloudflare Email Routing with D1, R2, and Durable Objects."
  keywords="emailflare, self-hosted email, cloudflare email sending, transactional email, open source email api, cloudflare inbox, team inbox cloudflare, email routing inbox, react email templates"
  canonical="https://emailflare.dev/"
  ogUrl="https://emailflare.dev/"
  ogType="website"
  navType="home"
>
  <Fragment slot="head">
    <script type="application/ld+json" set:html={JSON.stringify(structuredData)} />
  </Fragment>

<style is:global>
  /* ── HERO ─────────────────────────────────────────────────── */
  .hero {
    padding: clamp(4.5rem, 10vw, 8rem) 0 clamp(4rem, 8vw, 6.5rem);
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse 60% 70% at 72% 40%, oklch(93% 0.06 58 / 0.5) 0%, transparent 65%);
    pointer-events: none;
  }

  .hero-split {
    display: grid;
    grid-template-columns: 1fr 1.1fr;
    gap: clamp(2.5rem, 5vw, 5rem);
    align-items: center;
  }

  .hero-top-row {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 28px;
    flex-wrap: wrap;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    background: var(--orange-light);
    color: var(--orange-hover);
    font-size: 12.5px;
    font-weight: 500;
    padding: 4px 13px;
    border-radius: 100px;
    border: 1px solid var(--orange-mid);
  }

  .hero-sponsor {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    background: oklch(97% 0.02 10);
    color: oklch(48% 0.2 10);
    font-size: 12.5px;
    font-weight: 500;
    padding: 4px 13px;
    border-radius: 100px;
    border: 1px solid oklch(88% 0.06 10);
    text-decoration: none;
    transition: background 0.15s, border-color 0.15s;
  }
  .hero-sponsor:hover { background: oklch(93% 0.04 10); border-color: oklch(80% 0.1 10); }

  .hero h1 {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 4vw, 3.6rem);
    font-weight: 400;
    line-height: 1.08;
    letter-spacing: -0.028em;
    margin: 0 0 22px;
    color: var(--text);
  }

  .hero h1 em { font-style: italic; color: var(--orange); }

  .hero-sub {
    font-size: clamp(0.93rem, 1.7vw, 1.04rem);
    color: var(--text-muted);
    max-width: 460px;
    margin: 0 0 34px;
    line-height: 1.72;
    font-weight: 400;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
  }

  .hero-visual { position: relative; }

  @media (max-width: 860px) {
    .hero-split { grid-template-columns: 1fr; }
    .hero h1 { font-size: clamp(2.3rem, 7vw, 3.1rem); }
    .hero-sub { max-width: 520px; }
  }

  /* ── EMAIL FLOW DIAGRAM ────────────────────────────────────── */
  .email-flow-wrap {
    background: var(--bg-dark);
    border-radius: var(--radius-lg);
    box-shadow:
      0 32px 72px oklch(0% 0 0 / 0.22),
      0 0 0 1px oklch(100% 0 0 / 0.06),
      inset 0 1px 0 oklch(100% 0 0 / 0.04);
    overflow: hidden;
    padding: 28px 22px 24px;
  }

  .email-flow-svg {
    display: block;
    width: 100%;
    height: auto;
  }

  @keyframes ef-pulse {
    0%, 100% { opacity: 1; }
    50%       { opacity: 0.22; }
  }

  /* ── API SECTION ──────────────────────────────────────────── */
  .api-section {
    background: var(--bg-alt);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }

  .api-wrap {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(2rem, 5vw, 4rem);
    align-items: center;
  }

  @media (max-width: 720px) {
    .api-wrap { grid-template-columns: 1fr; }
  }

  .api-features {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 16px;
    margin-top: 32px;
  }

  .api-features li {
    display: flex;
    gap: 12px;
    align-items: flex-start;
  }

  .api-features .check {
    color: var(--orange);
    font-weight: 700;
    font-size: 14px;
    flex-shrink: 0;
    margin-top: 3px;
  }

  .api-features li .line {
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.6;
  }

  .api-features li strong {
    display: block;
    color: var(--text);
    font-weight: 500;
    font-size: 0.9375rem;
  }

  .code-window {
    background: var(--bg-dark);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow:
      0 16px 48px oklch(0% 0 0 / 0.16),
      0 0 0 1px oklch(100% 0 0 / 0.05);
  }

  .code-window-bar {
    background: var(--bg-dark-2);
    padding: 10px 16px;
    display: flex;
    align-items: center;
    gap: 7px;
  }

  .terminal-title {
    margin-left: auto;
    font-size: 11px;
    color: oklch(42% 0.01 58);
    font-family: var(--font-mono);
  }

  .code-window-body {
    padding: 20px 22px 22px;
    font-family: var(--font-mono);
    font-size: 12.5px;
    line-height: 1.75;
  }

  .c-method  { color: oklch(72% 0.14 230); font-weight: 600; }
  .c-path    { color: oklch(78% 0.10 80); }
  .c-header  { color: oklch(64% 0.01 58); }
  .c-key     { color: oklch(72% 0.14 230); }
  .c-str     { color: oklch(74% 0.12 85); }
  .c-punct   { color: oklch(56% 0.01 58); }
  .c-cmt     { color: oklch(44% 0.01 58); font-style: italic; }

  /* ── WHY SECTION ──────────────────────────────────────────── */
  .why-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 18px;
  }

  .why-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 26px 26px 22px;
    transition: border-color 0.15s;
  }

  .why-card:hover { border-color: var(--orange-mid); }

  .why-card .card-icon {
    font-size: 24px;
    line-height: 1;
    margin-bottom: 14px;
    display: block;
  }

  .why-card h3 {
    font-size: 0.975rem;
    font-weight: 600;
    margin-bottom: 8px;
    letter-spacing: -0.01em;
    line-height: 1.35;
  }

  .why-card p {
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.65;
  }

  /* ── VS SECTION ───────────────────────────────────────────── */
  .vs-section { background: var(--bg-alt); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }

  .vs-split {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 20px;
  }
  @media (max-width: 680px) { .vs-split { grid-template-columns: 1fr; } }

  .vs-card {
    border-radius: var(--radius-lg);
    overflow: hidden;
    border: 1px solid var(--border);
  }

  .vs-card-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 12px;
    padding: 20px 24px 18px;
    border-bottom: 1px solid var(--border);
  }
  .vs-card-cf .vs-card-header { background: oklch(95% 0.005 230); border-bottom-color: oklch(88% 0.01 230); }
  .vs-card-ef .vs-card-header { background: var(--orange-light); border-bottom-color: var(--orange-mid); }

  .vs-card-cf { background: var(--bg-card); }
  .vs-card-ef { background: oklch(99.5% 0.015 55); }

  .vs-header-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 4px;
  }

  .vs-title {
    font-size: 15px;
    font-weight: 700;
    letter-spacing: -0.01em;
  }
  .vs-card-cf .vs-title { color: oklch(35% 0.02 230); }
  .vs-card-ef .vs-title { color: var(--orange-hover); }

  .vs-subtitle { font-size: 12.5px; color: var(--text-faint); }

  .vs-badge {
    font-size: 10.5px;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: 3px 10px;
    border-radius: 99px;
    flex-shrink: 0;
  }
  .vs-badge-cf { background: oklch(89% 0.01 230); color: oklch(40% 0.02 230); border: 1px solid oklch(82% 0.015 230); }
  .vs-badge-ef { background: var(--orange); color: #fff; }

  .vs-rows { padding: 6px 0; }

  .vs-row {
    display: flex;
    align-items: baseline;
    gap: 10px;
    padding: 10px 24px;
    border-bottom: 1px solid var(--border);
    font-size: 13.5px;
    line-height: 1.45;
  }
  .vs-row:last-child { border-bottom: none; }
  .vs-card-cf .vs-row:hover { background: oklch(97.5% 0.005 230); }
  .vs-card-ef .vs-row:hover { background: oklch(98.5% 0.018 55); }

  .vs-header-brand {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .vs-icon { flex-shrink: 0; width: 18px; text-align: center; font-size: 13px; font-weight: 700; }
  .vs-card-cf .vs-icon { color: oklch(54% 0.16 24); }
  .vs-card-ef .vs-icon { color: oklch(50% 0.18 150); }

  .do-badge svg { width: 13px; height: 13px; display: inline-block; vertical-align: -2px; margin-right: 2px; }

  .vs-row-label { color: var(--text); }
  .vs-row code {
    font-family: var(--font-mono);
    font-size: 11.5px;
    background: oklch(93% 0.01 55);
    padding: 1px 5px;
    border-radius: 4px;
    color: var(--text);
  }

  /* ── FEATURES GRID ────────────────────────────────────────── */
  .features-grid {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 14px;
  }

  @media (max-width: 1100px) {
    .features-grid { grid-template-columns: repeat(3, 1fr); }
  }
  @media (max-width: 700px) {
    .features-grid { grid-template-columns: 1fr 1fr; }
  }
  @media (max-width: 480px) {
    .features-grid { grid-template-columns: 1fr; }
  }

  .feature-card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 18px 18px 16px;
    transition: border-color 0.15s;
  }

  .feature-card:hover { border-color: var(--border-strong); }

  .feature-card .card-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    border-radius: 9px;
    background: var(--orange-light);
    margin-bottom: 12px;
  }
  .feature-card .card-icon svg { width: 18px; height: 18px; color: var(--orange-hover); }

  .feature-card h3 {
    font-size: 0.9375rem;
    font-weight: 600;
    margin-bottom: 7px;
    letter-spacing: -0.01em;
  }

  .feature-card p {
    font-size: 0.875rem;
    color: var(--text-muted);
    line-height: 1.65;
  }

  .feature-card p code {
    font-family: var(--font-mono);
    font-size: 11.5px;
    background: oklch(93% 0.006 60);
    padding: 1px 5px;
    border-radius: 4px;
    color: var(--text);
  }

  .feature-tag {
    display: inline-block;
    font-size: 10.5px;
    font-weight: 500;
    color: var(--orange-hover);
    background: var(--orange-light);
    border: 1px solid var(--orange-mid);
    border-radius: 100px;
    padding: 2px 9px;
    margin-top: 12px;
  }

  /* ── LAYOUTS SHOWCASE ─────────────────────────────────────── */
  .layouts-section { background: var(--bg-alt); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }

  .layouts-scroll {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
    margin-top: 32px;
  }

  .layout-chip {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: 100px;
    padding: 6px 14px;
    font-size: 13px;
    color: var(--text-muted);
    font-weight: 450;
    transition: border-color 0.15s, color 0.15s;
  }

  .layout-chip::before {
    content: '';
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--orange-mid);
    flex-shrink: 0;
  }

  .layout-chip:hover { border-color: var(--orange-mid); color: var(--orange-hover); }

  /* ── DEPLOY SECTION ───────────────────────────────────────── */
  .deploy-section {
    background: var(--bg-dark);
    color: oklch(88% 0.01 58);
  }

  .deploy-section .section-eyebrow { color: var(--orange); }
  .deploy-section .section-title { color: oklch(95% 0.008 58); }
  .deploy-section .section-sub { color: oklch(60% 0.01 58); }

  /* two-column option cards */
  .deploy-options {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
    margin-top: 48px;
  }

  @media (max-width: 780px) {
    .deploy-options { grid-template-columns: 1fr; }
  }

  .deploy-option {
    background: var(--bg-dark-2);
    border: 1px solid oklch(26% 0.012 48);
    border-radius: var(--radius-lg);
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }

  .do-header {
    padding: 24px 28px 20px;
    border-bottom: 1px solid oklch(20% 0.012 48);
  }

  .do-badge {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    padding: 3px 10px;
    border-radius: 99px;
    margin-bottom: 14px;
  }

  .do-badge-docker {
    background: oklch(22% 0.04 230 / 0.5);
    color: oklch(68% 0.12 230);
    border: 1px solid oklch(34% 0.08 230 / 0.5);
  }

  .do-badge-worker {
    background: oklch(22% 0.06 48 / 0.5);
    color: var(--orange);
    border: 1px solid oklch(34% 0.10 48 / 0.5);
  }

  .do-title {
    font-size: 1.15rem;
    font-weight: 700;
    color: oklch(93% 0.01 58);
    margin-bottom: 8px;
  }

  .do-sub {
    font-size: 0.875rem;
    color: oklch(56% 0.01 58);
    line-height: 1.6;
  }

  .do-steps {
    padding: 20px 28px;
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .do-step {
    display: flex;
    gap: 14px;
    align-items: flex-start;
  }

  .do-step-num {
    flex-shrink: 0;
    width: 22px;
    height: 22px;
    border-radius: 50%;
    border: 1px solid oklch(30% 0.012 48);
    font-size: 11px;
    font-weight: 700;
    color: oklch(44% 0.01 58);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-top: 2px;
  }

  .do-step-label {
    font-size: 13px;
    font-weight: 600;
    color: oklch(84% 0.01 58);
    margin-bottom: 6px;
  }

  .do-step-desc {
    font-size: 12.5px;
    color: oklch(52% 0.01 58);
    line-height: 1.55;
    margin-bottom: 8px;
  }

  .do-footer {
    padding: 16px 28px;
    border-top: 1px solid oklch(20% 0.012 48);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
  }

  .do-footer-note {
    font-size: 12px;
    color: oklch(44% 0.01 58);
  }

  .do-footer a {
    font-size: 13px;
    font-weight: 600;
    text-decoration: none;
    white-space: nowrap;
  }

  .do-footer-docker a { color: oklch(62% 0.12 230); }
  .do-footer-worker a { color: var(--orange); }

  .code-block {
    background: oklch(10% 0.012 48);
    border: 1px solid oklch(22% 0.012 48);
    border-radius: 8px;
    padding: 12px 14px;
    font-family: var(--font-mono);
    font-size: 11.5px;
    line-height: 1.75;
    overflow-x: auto;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .cb-kw  { color: oklch(72% 0.14 165); }
  .cb-val { color: oklch(74% 0.12 82); }
  .cb-cmt { color: oklch(42% 0.01 58); font-style: italic; }
  .cb-cmd { color: oklch(80% 0.12 48); }
  .cb-url { color: var(--orange); }

  /* extra links below cards */
  .deploy-extras {
    margin-top: 20px;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 12px;
  }

  .deploy-extra {
    display: flex;
    align-items: center;
    gap: 12px;
    background: oklch(16% 0.012 48);
    border: 1px solid oklch(24% 0.012 48);
    border-radius: 10px;
    padding: 14px 18px;
    text-decoration: none;
    transition: border-color 0.15s;
  }

  .deploy-extra:hover { border-color: oklch(38% 0.02 48); }
  .de-icon { font-size: 20px; flex-shrink: 0; }
  .de-title { font-size: 13px; font-weight: 600; color: oklch(84% 0.01 58); margin-bottom: 2px; }
  .de-sub   { font-size: 12px; color: oklch(48% 0.01 58); }
  .de-arrow { margin-left: auto; color: oklch(40% 0.01 58); font-size: 13px; }

  /* ── ARCHITECTURE ─────────────────────────────────────────── */
  .arch-wrap {
    display: grid;
    grid-template-columns: 5fr 6fr;
    gap: clamp(2rem, 5vw, 4.5rem);
    align-items: center;
  }

  @media (max-width: 720px) {
    .arch-wrap { grid-template-columns: 1fr; }
  }

  .arch-diagram {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 24px;
    font-family: var(--font-mono);
    font-size: 11.5px;
  }

  .arch-box {
    border: 1px solid var(--border-strong);
    border-radius: 7px;
    padding: 10px 14px;
    margin-bottom: 10px;
    background: var(--bg-alt);
    text-align: center;
    font-size: 11px;
    color: var(--text-muted);
    line-height: 1.4;
  }

  .arch-box.hi {
    border-color: var(--orange-mid);
    background: var(--orange-light);
    color: var(--orange-hover);
    font-weight: 600;
  }

  .arch-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 8px;
    margin-bottom: 10px;
  }

  .arch-arrow { text-align: center; color: var(--text-faint); margin: -2px 0 2px; font-size: 12px; }

  .arch-bullets {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 18px;
  }

  .arch-bullets li {
    display: flex;
    gap: 14px;
    align-items: flex-start;
  }

  .ab-check {
    color: var(--orange);
    font-weight: 700;
    font-size: 14px;
    flex-shrink: 0;
    margin-top: 3px;
  }

  .arch-bullets li .ab-text strong {
    display: block;
    color: var(--text);
    font-weight: 500;
    font-size: 0.9375rem;
    margin-bottom: 3px;
  }

  .arch-bullets li .ab-text span {
    font-size: 0.875rem;
    color: var(--text-muted);
    line-height: 1.6;
  }

  /* ── OSS SECTION ──────────────────────────────────────────── */
  .oss-section {
    text-align: center;
    background: var(--bg-alt);
    border-top: 1px solid var(--border);
  }

  .oss-section .section-eyebrow { display: block; }
  .oss-section .section-title { margin: 0 auto 14px; }
  .oss-section .section-sub { margin: 0 auto 36px; }

  .oss-links {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    flex-wrap: wrap;
  }

  .built-on {
    margin-top: 56px;
    padding-top: 36px;
    border-top: 1px solid var(--border);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    font-size: 12.5px;
    color: var(--text-faint);
    flex-wrap: wrap;
  }

  .built-on a { color: var(--text-muted); text-decoration: none; }
  .built-on a:hover { color: var(--text); }
  .built-sep { color: var(--border-strong); }

  @media (max-width: 640px) {
    .why-grid { grid-template-columns: 1fr; }
  }

  /* ── PRODUCTS SPLIT ──────────────────────────────────────── */
  .products-section {
    background: var(--bg-alt);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }
  .products-section .section-eyebrow { color: var(--orange); }

  .products-split {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 24px;
  }
  @media (max-width: 760px) { .products-split { grid-template-columns: 1fr; } }

  .product-card {
    border-radius: var(--radius-lg);
    border: 1px solid var(--border);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    background: var(--bg-card);
    transition: border-color 0.15s;
  }
  .product-card-email:hover { border-color: var(--orange-mid); }
  .product-card-inbox:hover { border-color: oklch(70% 0.12 150 / 0.5); }

  .product-card-head { padding: 26px 26px 20px; flex: 1; }

  .product-eyebrow {
    display: inline-block;
    font-size: 10.5px; font-weight: 700;
    letter-spacing: 0.1em; text-transform: uppercase;
    padding: 3px 10px; border-radius: 100px;
    margin-bottom: 14px;
  }
  .product-eyebrow-email { background: var(--orange-light); color: var(--orange-hover); border: 1px solid var(--orange-mid); }
  .product-eyebrow-inbox { background: oklch(95% 0.04 150); color: oklch(34% 0.12 150); border: 1px solid oklch(82% 0.09 150 / 0.5); }

  .product-card-head h3 {
    font-family: var(--font-display);
    font-size: 1.25rem; font-weight: 400;
    letter-spacing: -0.02em; line-height: 1.2;
    margin-bottom: 10px;
  }
  .product-card-head p { font-size: 0.9rem; color: var(--text-muted); line-height: 1.65; }

  .product-feat-list {
    list-style: none; padding: 0 26px;
    margin: 0 0 4px;
    display: flex; flex-direction: column; gap: 9px;
  }
  .product-feat-list li {
    display: flex; align-items: baseline; gap: 9px;
    font-size: 0.875rem; color: var(--text-muted);
  }
  .pf-check { font-weight: 700; font-size: 12px; flex-shrink: 0; }
  .pf-check-email { color: var(--orange); }
  .pf-check-inbox { color: var(--green); }

  .product-card-foot {
    padding: 18px 26px;
    border-top: 1px solid var(--border);
    display: flex; align-items: center;
    justify-content: space-between;
    gap: 14px; flex-wrap: wrap;
  }
  .product-deploy-row { display: flex; align-items: center; gap: 7px; flex-wrap: wrap; }
  .product-deploy-label { font-size: 11.5px; color: var(--text-faint); }
  .product-badge {
    font-size: 11px; font-weight: 600;
    letter-spacing: 0.04em; text-transform: uppercase;
    padding: 2px 9px; border-radius: 99px;
  }
  .product-badge-docker { background: oklch(93% 0.02 230); color: oklch(40% 0.12 230); border: 1px solid oklch(80% 0.1 230 / 0.4); }
  .product-badge-worker { background: var(--orange-light); color: var(--orange-hover); border: 1px solid var(--orange-mid); }
  .product-badge-inbox-worker { background: oklch(95% 0.04 150); color: oklch(34% 0.12 150); border: 1px solid oklch(82% 0.09 150 / 0.5); }
  .product-badge-node { background: oklch(94% 0.03 80); color: oklch(38% 0.12 80); border: 1px solid oklch(80% 0.1 80 / 0.4); }
  .product-sep { font-size: 11px; color: var(--text-faint); }

  .btn-orange-sm {
    display: inline-flex; align-items: center; gap: 6px;
    background: var(--orange); color: #fff;
    border: 1.5px solid var(--orange);
    border-radius: 7px; padding: 7px 16px;
    font-size: 13px; font-weight: 500;
    text-decoration: none; white-space: nowrap;
    transition: background 0.15s;
  }
  .btn-orange-sm:hover { background: var(--orange-hover); border-color: var(--orange-hover); }

  .btn-green-sm {
    display: inline-flex; align-items: center; gap: 6px;
    background: var(--green); color: oklch(12% 0.02 150);
    border: 1.5px solid var(--green);
    border-radius: 7px; padding: 7px 16px;
    font-size: 13px; font-weight: 500;
    text-decoration: none; white-space: nowrap;
    transition: background 0.15s;
  }
  .btn-green-sm:hover { background: oklch(48% 0.18 150); border-color: oklch(48% 0.18 150); }

  /* ── INBOX PREVIEW ───────────────────────────────────────── */
  .inbox-preview-section {
    background: oklch(11% 0.025 150);
    border-top: 1px solid oklch(20% 0.03 150 / 0.6);
  }
  .inbox-preview-section .section-eyebrow { color: oklch(68% 0.14 150); }
  .inbox-preview-section .section-title   { color: oklch(95% 0.008 58); }
  .inbox-preview-section .section-title em { color: var(--green); font-style: italic; }
  .inbox-preview-section .section-sub     { color: oklch(56% 0.01 58); }

  .inbox-preview-split {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: clamp(2.5rem, 5vw, 4rem);
    align-items: center;
  }
  @media (max-width: 820px) { .inbox-preview-split { grid-template-columns: 1fr; } }

  .inbox-feat-list {
    list-style: none;
    display: flex; flex-direction: column; gap: 14px;
    margin: 24px 0 32px;
  }
  .inbox-feat-list li { display: flex; gap: 12px; align-items: flex-start; }
  .inbox-feat-list .ifl-check { color: var(--green); font-weight: 700; font-size: 14px; flex-shrink: 0; margin-top: 2px; }
  .inbox-feat-list li .ifl-text strong { display: block; color: oklch(88% 0.01 58); font-weight: 500; font-size: 0.9375rem; margin-bottom: 2px; }
  .inbox-feat-list li .ifl-text span { font-size: 0.875rem; color: oklch(54% 0.01 58); line-height: 1.6; }

  .btn-green-outline {
    display: inline-flex; align-items: center; gap: 6px;
    background: transparent;
    color: oklch(70% 0.14 150);
    border: 1.5px solid oklch(38% 0.12 150 / 0.5);
    border-radius: 7px; padding: 8px 18px;
    font-size: 13.5px; font-weight: 500;
    text-decoration: none; white-space: nowrap;
    transition: background 0.15s, border-color 0.15s, color 0.15s;
  }
  .btn-green-outline:hover {
    background: oklch(20% 0.06 150 / 0.4);
    border-color: oklch(56% 0.14 150 / 0.6);
    color: var(--green);
  }

  /* ── INBOX UI CARD (preview on index page) ───────────────── */
  .ix-ui-card {
    background: oklch(16% 0.02 150);
    border: 1px solid oklch(28% 0.06 150 / 0.5);
    border-radius: var(--radius-lg);
    overflow: hidden;
    box-shadow: 0 24px 60px oklch(0% 0 0 / 0.3), 0 0 0 1px oklch(100% 0 0 / 0.04);
  }
  .ix-ui-bar {
    background: oklch(20% 0.018 150);
    border-bottom: 1px solid oklch(28% 0.04 150 / 0.5);
    padding: 10px 16px;
    display: flex; align-items: center; gap: 7px;
  }
  .ix-dot { width: 10px; height: 10px; border-radius: 50%; }
  .ixd-r { background: oklch(52% 0.14 24 / 0.6); }
  .ixd-y { background: oklch(68% 0.14 80 / 0.6); }
  .ixd-g { background: oklch(56% 0.14 150 / 0.6); }
  .ix-bar-title { margin-left: 6px; font-size: 11px; color: oklch(44% 0.01 58); font-family: var(--font-mono); }
  .ix-ui-body { display: flex; }
  .ix-ui-list { width: 44%; border-right: 1px solid oklch(22% 0.02 150 / 0.7); padding: 8px 0; }
  .ix-contact-row { padding: 10px 14px; border-bottom: 1px solid oklch(20% 0.01 150 / 0.5); cursor: default; position: relative; }
  .ix-contact-row.ix-active { background: oklch(22% 0.06 150 / 0.4); }
  .ix-contact-row.ix-active::before { content: ''; position: absolute; left: 0; top: 8px; bottom: 8px; width: 3px; border-radius: 0 3px 3px 0; background: var(--green); }
  .ix-row-top { display: flex; align-items: center; gap: 8px; margin-bottom: 4px; }
  .ix-avatar { width: 24px; height: 24px; border-radius: 50%; background: oklch(28% 0.06 150 / 0.6); display: flex; align-items: center; justify-content: center; font-size: 9px; font-weight: 600; color: oklch(70% 0.1 150); flex-shrink: 0; }
  .ix-avatar.ix-unread { background: oklch(32% 0.1 150 / 0.7); }
  .ix-name { font-size: 11px; font-weight: 600; color: oklch(84% 0.01 58); flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .ix-time { font-size: 9.5px; color: oklch(44% 0.01 58); font-family: var(--font-mono); }
  .ix-preview { font-size: 10px; color: oklch(48% 0.01 58); line-height: 1.5; padding-left: 32px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .ix-unread-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--green); flex-shrink: 0; }
  .ix-ui-thread { flex: 1; padding: 14px; display: flex; flex-direction: column; gap: 8px; }
  .ix-thread-header { display: flex; align-items: center; gap: 10px; padding-bottom: 10px; border-bottom: 1px solid oklch(22% 0.02 150 / 0.7); margin-bottom: 4px; }
  .ix-thread-avatar { width: 30px; height: 30px; border-radius: 50%; background: oklch(30% 0.08 150 / 0.6); display: flex; align-items: center; justify-content: center; font-size: 11px; font-weight: 700; color: oklch(72% 0.12 150); flex-shrink: 0; }
  .ix-thread-name { font-size: 12.5px; font-weight: 600; color: oklch(88% 0.01 58); }
  .ix-thread-sub  { font-size: 10px; color: oklch(48% 0.01 58); }
  .ix-thread-badge { margin-left: auto; font-size: 9.5px; font-weight: 600; background: oklch(26% 0.08 150 / 0.5); color: oklch(68% 0.1 150); border: 1px solid oklch(38% 0.1 150 / 0.4); padding: 1px 7px; border-radius: 100px; }
  .ix-email-card { background: oklch(18% 0.016 150 / 0.8); border: 1px solid oklch(24% 0.03 150 / 0.5); border-radius: 7px; padding: 10px 12px; }
  .ix-email-card.ix-sent { background: oklch(22% 0.06 150 / 0.3); border-color: oklch(32% 0.08 150 / 0.4); }
  .ix-email-header { display: flex; align-items: center; gap: 7px; margin-bottom: 6px; }
  .ix-email-from-badge { font-size: 9.5px; font-weight: 600; padding: 1px 6px; border-radius: 100px; background: oklch(24% 0.05 150 / 0.5); color: oklch(62% 0.08 150); border: 1px solid oklch(32% 0.06 150 / 0.4); }
  .ix-email-card.ix-sent .ix-email-from-badge { background: oklch(28% 0.1 150 / 0.4); color: oklch(72% 0.12 150); border-color: oklch(42% 0.1 150 / 0.4); }
  .ix-email-subj { font-size: 10px; font-weight: 600; color: oklch(80% 0.01 58); flex: 1; }
  .ix-email-ts   { font-size: 9px; color: oklch(42% 0.01 58); font-family: var(--font-mono); margin-left: auto; }
  .ix-email-body { font-size: 9.5px; color: oklch(55% 0.01 58); line-height: 1.6; }
  .ix-reply-bar { background: oklch(18% 0.018 150); border: 1px solid oklch(28% 0.05 150 / 0.4); border-radius: 7px; padding: 8px 12px; margin-top: 4px; display: flex; align-items: center; gap: 8px; font-size: 10px; color: oklch(38% 0.01 58); }
  .ix-reply-btn { margin-left: auto; background: var(--green); color: oklch(12% 0.02 150); font-size: 9.5px; font-weight: 600; padding: 3px 10px; border-radius: 5px; white-space: nowrap; }

  /* ── DEPLOY PRODUCT GROUPS ───────────────────────────────── */
  .deploy-product-group-label {
    display: flex; align-items: center; gap: 14px;
    margin-bottom: 20px;
  }
  .deploy-product-group-label-badge {
    font-size: 11px; font-weight: 700;
    letter-spacing: 0.06em; text-transform: uppercase;
    padding: 4px 14px; border-radius: 99px;
    white-space: nowrap;
  }
  .deploy-product-group-label-badge-email { background: oklch(22% 0.06 48 / 0.5); color: var(--orange); border: 1px solid oklch(34% 0.10 48 / 0.5); }
  .deploy-product-group-label-badge-inbox { background: oklch(20% 0.06 150 / 0.4); color: oklch(68% 0.14 150); border: 1px solid oklch(32% 0.1 150 / 0.4); }
  .deploy-product-divider { flex: 1; height: 1px; background: oklch(24% 0.012 48); }

  .do-badge-inbox-worker { background: oklch(20% 0.06 150 / 0.5); color: oklch(68% 0.12 150); border: 1px solid oklch(32% 0.1 150 / 0.4); }
  .do-badge-node { background: oklch(22% 0.04 80 / 0.5); color: oklch(70% 0.12 80); border: 1px solid oklch(36% 0.10 80 / 0.4); }
  .do-footer-inbox-worker a { color: oklch(62% 0.14 150); }
  .do-footer-node a { color: oklch(70% 0.12 80); }

  /* ── PRODUCT TAB SWITCHER ───────────────────────────────── */
  .psw-wrap {
    background: oklch(10% 0.012 48);
    border-bottom: 1px solid var(--border);
    position: sticky;
    top: 0;
    z-index: 50;
    padding: 0;
  }
  .psw-inner {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 clamp(1.25rem, 5vw, 3rem);
    display: flex;
    gap: 0;
  }
  .psw-btn {
    padding: 14px 28px 12px;
    border: none;
    border-bottom: 2.5px solid transparent;
    background: transparent;
    cursor: pointer;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1px;
    transition: opacity 0.15s;
    opacity: 0.45;
  }
  .psw-btn:hover { opacity: 0.75; }
  .psw-btn.psw-active { opacity: 1; }
  .psw-email.psw-active { border-bottom-color: var(--orange); }
  .psw-inbox.psw-active { border-bottom-color: var(--green); }
  .psw-label {
    font-size: 13.5px; font-weight: 600;
    color: oklch(88% 0.01 58);
    letter-spacing: -0.01em;
    font-family: var(--font-display);
  }
  .psw-sub { font-size: 10px; color: var(--text-faint); }
  /* ── LAYOUTS TEASER ─────────────────────────────────────── */
  .layouts-teaser-section { background: var(--bg-alt); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }

  /* ── DEPLOY CTA ──────────────────────────────────────────── */
  .deploy-cta-section { background: var(--bg-dark); border-top: 1px solid var(--border); }
  .deploy-cta-section .section-eyebrow { color: var(--orange); }
  .deploy-cta-section .section-title { color: oklch(95% 0.008 58); }
  .deploy-cta-section .section-sub { color: oklch(60% 0.01 58); }
  .deploy-cta-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; }
  @media (max-width: 680px) { .deploy-cta-grid { grid-template-columns: 1fr; } }
  .deploy-cta-card {
    background: var(--bg-dark-2); border-radius: var(--radius-lg); padding: 28px 32px;
    text-decoration: none; display: flex; flex-direction: column; gap: 8px; transition: border-color 0.15s;
  }
  .deploy-cta-email { border: 1px solid oklch(30% 0.08 48 / 0.5); }
  .deploy-cta-email:hover { border-color: var(--orange); }
  .deploy-cta-inbox { border: 1px solid oklch(28% 0.08 150 / 0.4); }
  .deploy-cta-inbox:hover { border-color: var(--green); }
  .dcc-eyebrow { font-size: 10.5px; font-weight: 700; letter-spacing: 0.1em; text-transform: uppercase; color: var(--orange); }
  .dcc-title { font-size: 1.1rem; font-weight: 700; color: oklch(93% 0.01 58); font-family: var(--font-display); letter-spacing: -0.01em; }
  .dcc-sub { font-size: 0.875rem; color: oklch(56% 0.01 58); line-height: 1.6; }
  .dcc-sub code { background: oklch(18% 0.02 48); border: 1px solid oklch(28% 0.02 48); color: oklch(72% 0.1 80); padding: 1px 5px; border-radius: 4px; font-size: 11.5px; }
  .dcc-cta { margin-top: 10px; font-size: 13px; font-weight: 600; color: var(--orange); }
</style>

  <!-- HERO -->
  <section class="hero">
    <div class="container">
      <div class="hero-split">

        <!-- LEFT: headline + CTAs -->
        <div class="hero-text">
          <div class="hero-top-row">
            <div class="hero-badge">Open Source · MIT License</div>
            <a class="hero-sponsor" href="https://github.com/sponsors/0xdps" target="_blank" rel="noopener">♥ Sponsor</a>
          </div>

          <h1>
            <em>Email API</em> and <em style="color:var(--green)">inbox</em><br />
            for your Cloudflare stack.
          </h1>

          <p class="hero-sub">
            Two open-source products in one repo — a REST email sending API with React Email
            templates, and a full team inbox built on Cloudflare Email Routing.
            Both deploy as a Cloudflare Worker or self-hosted Docker container.
          </p>

          <div class="hero-actions">
            <a class="btn btn-primary" href="/email-api">Email API →</a>
            <a class="btn btn-secondary" href="/inbox">Inbox →</a>
            <a class="btn btn-secondary" href="#deploy">Self-host</a>
            <a class="btn btn-secondary" href="https://github.com/0xdps/emailflare" target="_blank" rel="noopener">
              <svg width="15" height="15" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z"/></svg>
              View on GitHub
              <span id="gh-stars-hero" style="display:inline-flex;align-items:center;gap:3px;margin-left:4px;padding:1px 7px;border-radius:100px;background:oklch(93% 0.006 60);border:1px solid oklch(82% 0.012 58);font-size:11.5px;font-weight:600;color:oklch(40% 0.012 58);font-variant-numeric:tabular-nums;">&#9733; 100</span>
            </a>
          </div>
        </div>

        <!-- RIGHT: animated email flow -->
        <div class="hero-visual">
          <div class="email-flow-wrap">
            <svg class="email-flow-svg" viewBox="0 0 500 490" xmlns="http://www.w3.org/2000/svg" aria-label="Email flow: Your App sends via EmailFlare through Cloudflare Email Sending to the recipient Inbox">
              <defs>
                <marker id="ef-arr" viewBox="0 0 10 7" refX="9" refY="3.5" markerUnits="strokeWidth" markerWidth="7" markerHeight="5" orient="auto">
                  <path d="M0,0 L10,3.5 L0,7 Z" fill="#5a5040"/>
                </marker>
                <radialGradient id="ef-glow" cx="50%" cy="50%" r="50%">
                  <stop offset="0%" stop-color="#ea580c" stop-opacity="0.26"/>
                  <stop offset="100%" stop-color="#ea580c" stop-opacity="0"/>
                </radialGradient>
                <linearGradient id="ef-logo-bg" x1="0" y1="1" x2="1" y2="0">
                  <stop offset="0%" stop-color="#9a3412"/>
                  <stop offset="58%" stop-color="#ea580c"/>
                  <stop offset="100%" stop-color="#fb923c"/>
                </linearGradient>
                <!-- connector paths for animateMotion -->
                <path id="p1" d="M158,74 L318,74"/>
                <path id="p2" d="M400,136 L400,338"/>
                <path id="p3" d="M318,399 L160,399"/>
              </defs>

              <!-- ambient glow behind EmailFlare node -->
              <ellipse cx="400" cy="70" rx="140" ry="100" fill="url(#ef-glow)"/>

              <!-- ── NODE 1: Your App (top-left) ──────────────────── -->
              <rect x="10" y="14" width="148" height="118" rx="10" fill="#1d1b18" stroke="#332f28" stroke-width="1"/>
              <!-- terminal window icon -->
              <rect x="52" y="28" width="52" height="38" rx="5" fill="#2a2620" stroke="#3e3a32" stroke-width="1"/>
              <circle cx="60" cy="36" r="2.5" fill="#ff5f57"/>
              <circle cx="68" cy="36" r="2.5" fill="#ffbd2e"/>
              <circle cx="76" cy="36" r="2.5" fill="#28c840"/>
              <polyline points="55,50 62,55 55,60" fill="none" stroke="#6a9955" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
              <line x1="65" y1="60" x2="78" y2="60" stroke="#5a8a8a" stroke-width="1.6" stroke-linecap="round"/>
              <text x="84" y="88" text-anchor="middle" font-family="DM Sans, system-ui, sans-serif" font-size="13" font-weight="600" fill="#c0b4a4">Your App</text>
              <text x="84" y="106" text-anchor="middle" font-family="DM Sans, system-ui, sans-serif" font-size="11" fill="#6a6050">Any language</text>

              <!-- ── CONNECTOR 1: right (App → EmailFlare) ────────── -->
              <line x1="158" y1="74" x2="318" y2="74" stroke="#504840" stroke-width="1.5" marker-end="url(#ef-arr)"/>
              <circle r="3.5" fill="#ea580c" opacity="0.9">
                <animateMotion dur="1.8s" repeatCount="indefinite" begin="0s"><mpath href="#p1"/></animateMotion>
              </circle>
              <circle r="3.5" fill="#ea580c" opacity="0.9">
                <animateMotion dur="1.8s" repeatCount="indefinite" begin="0.9s"><mpath href="#p1"/></animateMotion>
              </circle>
              <text x="238" y="61" text-anchor="middle" font-family="SF Mono, Fira Code, monospace" font-size="9.5" fill="#9c8c78">POST /v1/send</text>

              <!-- ── NODE 2: EmailFlare (top-right, hero node) ──────── -->
              <rect x="316" y="4" width="166" height="132" rx="14" fill="none" stroke="#ea580c" stroke-width="1.5" style="animation: ef-pulse 2.8s ease-in-out infinite;"/>
              <rect x="318" y="6" width="162" height="128" rx="12" fill="#180c02" stroke="#9a3412" stroke-width="1"/>
              <!-- EmailFlare logo: orange-gradient rounded square + two white triangles -->
              <rect x="378" y="18" width="44" height="44" rx="9" fill="url(#ef-logo-bg)"/>
              <path d="M412,26 L383,37 L391,43 Z" fill="white"/>
              <path d="M412,26 L391,43 L383,54 Z" fill="white" opacity="0.82"/>
              <text x="399" y="84" text-anchor="middle" font-family="DM Sans, system-ui, sans-serif" font-size="13.5" font-weight="600" fill="#ecd4b0">EmailFlare</text>
              <text x="399" y="102" text-anchor="middle" font-family="DM Sans, system-ui, sans-serif" font-size="11" fill="#a85828">Render · Log · Route</text>

              <!-- ── CONNECTOR 2: down (EmailFlare → Cloudflare) ────── -->
              <line x1="400" y1="136" x2="400" y2="338" stroke="#504840" stroke-width="1.5" marker-end="url(#ef-arr)"/>
              <circle r="3.5" fill="#c05818" opacity="0.9">
                <animateMotion dur="1.6s" repeatCount="indefinite" begin="0.3s"><mpath href="#p2"/></animateMotion>
              </circle>
              <circle r="3.5" fill="#c05818" opacity="0.9">
                <animateMotion dur="1.6s" repeatCount="indefinite" begin="1.1s"><mpath href="#p2"/></animateMotion>
              </circle>
              <text x="418" y="237" text-anchor="start" font-family="SF Mono, Fira Code, monospace" font-size="9.5" fill="#9c8c78">SMTP · route</text>

              <!-- ── NODE 3: Cloudflare Email (bottom-right) ────────── -->
              <rect x="318" y="340" width="162" height="118" rx="10" fill="#080f1c" stroke="#1a3660" stroke-width="1"/>
              <!-- Cloudflare logo SVG (128×128 viewBox), scaled to ~56px, centered in node -->
              <g transform="translate(371,352) scale(0.44)">
                <path fill="#FFF" d="m115.679 69.288-15.591-8.94-2.689-1.163-63.781.436v32.381h82.061z"/>
                <path fill="#F38020" d="M87.295 89.022c.763-2.617.472-5.015-.8-6.796-1.163-1.635-3.125-2.58-5.488-2.689l-44.737-.581c-.291 0-.545-.145-.691-.363s-.182-.509-.109-.8c.145-.436.581-.763 1.054-.8l45.137-.581c5.342-.254 11.157-4.579 13.192-9.885l2.58-6.723c.109-.291.145-.581.073-.872-2.906-13.158-14.644-22.97-28.672-22.97-12.938 0-23.913 8.359-27.838 19.952a13.35 13.35 0 0 0-9.267-2.58c-6.215.618-11.193 5.597-11.811 11.811-.145 1.599-.036 3.162.327 4.615C10.104 70.051 2 78.337 2 88.549c0 .909.073 1.817.182 2.726a.895.895 0 0 0 .872.763h82.57c.472 0 .909-.327 1.054-.8l.617-2.216z"/>
                <path fill="#FAAE40" d="M101.542 60.275c-.4 0-.836 0-1.236.036-.291 0-.545.218-.654.509l-1.744 6.069c-.763 2.617-.472 5.015.8 6.796 1.163 1.635 3.125 2.58 5.488 2.689l9.522.581c.291 0 .545.145.691.363.145.218.182.545.109.8-.145.436-.581.763-1.054.8l-9.924.582c-5.379.254-11.157 4.579-13.192 9.885l-.727 1.853c-.145.363.109.727.509.727h34.089c.4 0 .763-.254.872-.654.581-2.108.909-4.325.909-6.614 0-13.447-10.975-24.422-24.458-24.422"/>
              </g>
              <text x="399" y="428" text-anchor="middle" font-family="DM Sans, system-ui, sans-serif" font-size="13" font-weight="600" fill="#88aadc">Cloudflare Email</text>
              <text x="399" y="446" text-anchor="middle" font-family="DM Sans, system-ui, sans-serif" font-size="11" fill="#4a72b0">Sending API</text>

              <!-- ── CONNECTOR 3: left (Cloudflare → Inbox) ────────── -->
              <line x1="318" y1="399" x2="160" y2="399" stroke="#504840" stroke-width="1.5" marker-end="url(#ef-arr)"/>
              <circle r="3.5" fill="#3878c0" opacity="0.9">
                <animateMotion dur="1.5s" repeatCount="indefinite" begin="0.15s"><mpath href="#p3"/></animateMotion>
              </circle>
              <circle r="3.5" fill="#3878c0" opacity="0.9">
                <animateMotion dur="1.5s" repeatCount="indefinite" begin="0.9s"><mpath href="#p3"/></animateMotion>
              </circle>
              <text x="238" y="386" text-anchor="middle" font-family="SF Mono, Fira Code, monospace" font-size="9.5" fill="#9c8c78">delivered</text>

              <!-- ── NODE 4: Inbox (bottom-left) ──────────────────── -->
              <rect x="10" y="340" width="148" height="118" rx="10" fill="#071310" stroke="#1a3c28" stroke-width="1"/>
              <!-- Inbox icon (48×48) -->
              <svg x="54" y="348" width="60" height="60" viewBox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
                <path d="M38.8824,13.85055v7.8501c0,1.2-0.99,2.19-2.2,2.19h-7.85c-0.92,0-1.71-0.57-2.03-1.37c-0.1-0.25-0.16-0.5301-0.16-0.82v-7.8501c0-1.2099,0.98-2.1999,2.19-2.1999h7.85C37.8924,11.65065,38.8824,12.64065,38.8824,13.85055z" style="fill:#F43735;"/>
                <polygon points="25.4376,16.34935 25.4376,21.92785 21.8176,24.46385 10.0547,16.34935" style="fill:#4AA0EC;"/>
                <path d="M34.5176,25.08935v8.44h-22.41l-2.99,2.82v-19.08l12.7,8.7664l4-2.7964c0.57,1.13,1.72,1.85,3.01,1.85H34.5176z" style="fill:#4AA0EC;"/>
                <path d="M33.0324,15.17065l-1.14,0.61l0.14,0.54l0.9-0.49h0.02v4.54h0.68v-5.2H33.0324L33.0324,15.17065z" style="fill:#FFFFFF;"/>
              </svg>
              <text x="84" y="424" text-anchor="middle" font-family="DM Sans, system-ui, sans-serif" font-size="13" font-weight="600" fill="#7acc8e">Inbox</text>
              <text x="84" y="441" text-anchor="middle" font-family="DM Sans, system-ui, sans-serif" font-size="11" fill="#2e7040">Delivered ✓</text>
            </svg>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- PRODUCTS -->
  <section id="products">
    <div class="container">
      <div class="section-head">
        <div class="section-eyebrow">Two products, one repo</div>
        <h2 class="section-title">Pick what you need.<br />Or run both.</h2>
        <p class="section-sub">EmailFlare ships two independent products that share the same Cloudflare infrastructure. Use one, the other, or both together.</p>
      </div>
      <div class="products-split">
        <!-- Email API card -->
        <div class="product-card product-card-email">
          <div class="product-card-head">
            <span class="product-eyebrow product-eyebrow-email">Email API</span>
            <h3>Transactional email<br />on Cloudflare.</h3>
            <p>A REST email sending API with scoped keys, React Email templates, domain management, send history, and a full admin UI — one Docker image, zero external services.</p>
          </div>
          <ul class="product-feat-list">
            <li><span class="pf-check pf-check-email">✓</span>POST /v1/send — one endpoint, any language</li>
            <li><span class="pf-check pf-check-email">✓</span>50 React Email layouts · 5 built-in themes</li>
            <li><span class="pf-check pf-check-email">✓</span>Scoped API keys, rate limiting, idempotency</li>
            <li><span class="pf-check pf-check-email">✓</span>Full send log with per-message CF delivery IDs</li>
          </ul>
          <div class="product-card-foot">
            <div class="product-deploy-row">
              <span class="product-deploy-label">Deploy via</span>
              <span class="product-badge product-badge-docker">Docker</span>
              <span class="product-sep">·</span>
              <span class="product-badge product-badge-worker">Workers</span>
            </div>
            <a href="/email-api" class="btn-orange-sm">Learn more →</a>
          </div>
        </div>
        <!-- Inbox card -->
        <div class="product-card product-card-inbox">
          <div class="product-card-head">
            <span class="product-eyebrow product-eyebrow-inbox">Inbox</span>
            <h3>Team email inbox<br />on Cloudflare Routing.</h3>
            <p>A multi-user inbox powered by Cloudflare Email Routing. Receive, reply, assign threads, and build auto-reply sequences — without managing a mail server.</p>
          </div>
          <ul class="product-feat-list">
            <li><span class="pf-check pf-check-inbox">✓</span>Receive email via Cloudflare Email Routing</li>
            <li><span class="pf-check pf-check-inbox">✓</span>Shared team inbox · thread assignment</li>
            <li><span class="pf-check pf-check-inbox">✓</span>Auto-reply sequences with delivery tracking</li>
            <li><span class="pf-check pf-check-inbox">✓</span>Workers + Node.js deploy — zero mail server</li>
          </ul>
          <div class="product-card-foot">
            <div class="product-deploy-row">
              <span class="product-deploy-label">Deploy via</span>
              <span class="product-badge product-badge-inbox-worker">Workers</span>
              <span class="product-sep">·</span>
              <span class="product-badge product-badge-node">Node.js</span>
            </div>
            <a href="/inbox" class="btn-green-sm">Learn more →</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- TEMPLATES TEASER -->
  <section class="layouts-teaser-section">
    <div class="container">
      <div class="section-head">
        <div class="section-eyebrow">React Email Layouts</div>
        <h2 class="section-title">50 email layouts.<br />Ready on day one.</h2>
        <p class="section-sub">EmailFlare ships with 50 production-ready React Email layouts — from Welcome and Magic Link to Invoice, Incident Update, and more. Switch between 5 built-in themes per send.</p>
      </div>
      <div class="layouts-scroll">
        <span class="layout-chip">Welcome</span>
        <span class="layout-chip">Magic Link</span>
        <span class="layout-chip">OTP Code</span>
        <span class="layout-chip">Email Verify</span>
        <span class="layout-chip">Password Reset</span>
        <span class="layout-chip">Order Confirmation</span>
        <span class="layout-chip">Invoice</span>
        <span class="layout-chip">Subscription Started</span>
        <span class="layout-chip">Trial Ending</span>
        <span class="layout-chip">Payment Failed</span>
        <span class="layout-chip">Team Invite</span>
        <span class="layout-chip">Alert</span>
        <span class="layout-chip">Billing Receipt</span>
        <span class="layout-chip">Incident Update</span>
        <span class="layout-chip">Support Ticket Reply</span>
        <span class="layout-chip">New Login Alert</span>
        <span class="layout-chip">Account Locked</span>
        <span class="layout-chip">Announcement</span>
        <span class="layout-chip">Digest</span>
        <span class="layout-chip">Feedback Request</span>
      </div>
      <div style="margin-top:24px;">
        <a class="btn btn-secondary" href="/email-api#templates">See all 50 layouts →</a>
      </div>
    </div>
  </section>

  <!-- DEPLOY -->
  <section id="deploy" class="deploy-cta-section">
    <div class="container">
      <div class="section-head">
        <div class="section-eyebrow">Self-hosting</div>
        <h2 class="section-title">Deploy in minutes.</h2>
        <p class="section-sub">Both products ship with Docker and Cloudflare Workers support. Pick a deploy path — both are in the same repo.</p>
      </div>
      <div class="deploy-cta-grid">
        <a href="/email-api#deploy" class="deploy-cta-card deploy-cta-email">
          <div class="dcc-eyebrow">Email API</div>
          <div class="dcc-title">Docker · Cloudflare Workers</div>
          <p class="dcc-sub">One image — Hono API + React admin + SQLite. Or deploy serverless on Cloudflare Workers with D1 + KV. <code>just worker-setup</code> handles provisioning.</p>
          <div class="dcc-cta">View deploy options →</div>
        </a>
        <a href="/inbox#deploy" class="deploy-cta-card deploy-cta-inbox">
          <div class="dcc-eyebrow" style="color:var(--green)">Inbox</div>
          <div class="dcc-title">Cloudflare Workers · Node.js</div>
          <p class="dcc-sub">Workers deploy uses D1 + Email Routing Worker binding. Node.js deploy runs the same Hono API in a container. <code>just inbox-setup</code> sets everything up.</p>
          <div class="dcc-cta" style="color:var(--green)">View deploy options →</div>
        </a>
      </div>
    </div>
  </section>

  <!-- ARCHITECTURE -->
  <section id="architecture">
    <div class="container">
      <div class="section-head">
        <div class="section-eyebrow">Architecture</div>
        <h2 class="section-title">Minimal by design.</h2>
        <p class="section-sub">
          EmailFlare is intentionally simple to operate. No orchestration overhead,
          no extra services, no surprise dependencies.
        </p>
      </div>
      <div class="arch-wrap">
        <div class="arch-diagram">
          <div class="arch-box">Your application / HTTP client</div>
          <div class="arch-arrow">↓</div>
          <div class="arch-box">Caddy — edge proxy</div>
          <div class="arch-arrow">↓ &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; ↓</div>
          <div class="arch-row">
            <div class="arch-box hi">Hono API<br /><small style="font-weight:400;font-size:10px;">:3000</small></div>
            <div class="arch-box hi">React Admin<br /><small style="font-weight:400;font-size:10px;">:80</small></div>
          </div>
          <div class="arch-arrow">↓</div>
          <div class="arch-row">
            <div class="arch-box">SQLite<br /><small style="font-size:10px;">mesahub-core</small></div>
            <div class="arch-box">Cloudflare<br /><small style="font-size:10px;">Email Sending API</small></div>
          </div>
        </div>

        <ul class="arch-bullets">
          <li>
            <span class="ab-check">✓</span>
            <div class="ab-text">
              <strong>No Postgres or Redis</strong>
              <span>SQLite runs embedded via mesahub-core. All data persists in the Docker volume at <code>/data</code>. No external database to provision or back up separately.</span>
            </div>
          </li>
          <li>
            <span class="ab-check">✓</span>
            <div class="ab-text">
              <strong>Three services, one image</strong>
              <span>The production Docker image bundles the Hono API backend, React admin UI, Mailpit, and embedded mesahub storage. Mailpit only starts when <code>ENABLE_TEST_MODE=true</code> — zero overhead in a standard production deploy.</span>
            </div>
          </li>
          <li>
            <span class="ab-check">✓</span>
            <div class="ab-text">
              <strong>Cloudflare Email Sending for production</strong>
              <span>Production API keys send exclusively through Cloudflare's infrastructure. Test API keys route through SMTP for safe testing without CF credentials.</span>
            </div>
          </li>
          <li>
            <span class="ab-check">✓</span>
            <div class="ab-text">
              <strong>Scale out storage when you need to</strong>
              <span>Replace <code>MESAHUB_URL</code> with an external mesahub instance if you ever need to split storage out of the app container.</span>
            </div>
          </li>
          <li>
            <span class="ab-check">✓</span>
            <div class="ab-text">
              <strong>Schema bootstrapped at startup</strong>
              <span>All tables are created with <code>CREATE TABLE IF NOT EXISTS</code> on first boot. No migration framework, no manual schema setup, no CLI commands to run.</span>
            </div>
          </li>
        </ul>
      </div>
    </div>
  </section>

  <!-- OPEN SOURCE -->
  <section class="oss-section">
    <div class="container">
      <div class="section-eyebrow">Open Source</div>
      <h2 class="section-title">Built in public. MIT licensed.</h2>
      <p class="section-sub">
        EmailFlare is fully open source. Read the code, contribute, file issues,
        or fork it for your own needs — no strings attached.
      </p>
      <div class="oss-links">
        <a class="btn btn-primary" href="https://github.com/0xdps/emailflare" target="_blank" rel="noopener">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z"/></svg>
          View on GitHub
          <span id="gh-stars-oss" style="display:inline-flex;align-items:center;gap:3px;margin-left:4px;padding:1px 7px;border-radius:100px;background:oklch(100% 0 0 / 0.12);border:1px solid oklch(100% 0 0 / 0.18);font-size:11.5px;font-weight:600;color:oklch(88% 0.01 58);font-variant-numeric:tabular-nums;">★ 100</span>
        </a>
        <a class="btn btn-secondary" href="https://github.com/0xdps/emailflare/blob/trunk/CONTRIBUTING.md" target="_blank" rel="noopener">Contributing guide</a>
        <a class="btn btn-secondary" href="https://github.com/0xdps/emailflare/blob/trunk/docs/SELF_HOSTING.md" target="_blank" rel="noopener">Self-hosting docs</a>
        <a class="btn btn-secondary" href="https://github.com/0xdps/emailflare/blob/trunk/SECURITY.md" target="_blank" rel="noopener">Security policy</a>
        <a class="btn btn-sponsor" href="https://github.com/sponsors/0xdps" target="_blank" rel="noopener">♥ Sponsor</a>
      </div>

      <div class="built-on">
        <span>Built with</span>
        <a href="https://hono.dev" target="_blank" rel="noopener">Hono</a>
        <span class="built-sep">·</span>
        <a href="https://react.dev" target="_blank" rel="noopener">React 19</a>
        <span class="built-sep">·</span>
        <a href="https://react.email" target="_blank" rel="noopener">React Email</a>
        <span class="built-sep">·</span>
        <a href="https://vitejs.dev" target="_blank" rel="noopener">Vite</a>
        <span class="built-sep">·</span>
        <a href="https://tanstack.com/router" target="_blank" rel="noopener">TanStack Router</a>
        <span class="built-sep">·</span>
        <a href="https://caddyserver.com" target="_blank" rel="noopener">Caddy</a>
        <span class="built-sep">·</span>
        <span>Cloudflare Email Sending</span>
      </div>
    </div>
  </section>

  <ul slot="footer-links" class="footer-links">
    <li><a href="https://github.com/0xdps/emailflare" target="_blank" rel="noopener">GitHub</a></li>
    <li><a href="/docs">Email API docs</a></li>
    <li><a href="/inbox">Inbox</a></li>
    <li><a href="https://github.com/0xdps/emailflare/blob/trunk/SECURITY.md" target="_blank" rel="noopener">Security</a></li>
    <li><a href="https://github.com/0xdps/emailflare/blob/trunk/LICENSE" target="_blank" rel="noopener">License</a></li>
  </ul>

  <script slot="scripts" is:inline>
    function runStarCount() {
      function animateStars(target) {
        var start = 125;
        var duration = 1000;
        var startTime = null;
        var els = [
          document.getElementById('gh-stars-hero'),
          document.getElementById('gh-stars-oss')
        ];
        function easeOut(t) { return 1 - Math.pow(1 - t, 3); }
        function step(ts) {
          if (!startTime) startTime = ts;
          var progress = Math.min((ts - startTime) / duration, 1);
          var current = Math.round(start + (target - start) * easeOut(progress));
          var label = '\u2605 ' + current;
          els.forEach(function(el) { if (el) el.textContent = label; });
          if (progress < 1) requestAnimationFrame(step);
        }
        requestAnimationFrame(step);
      }
      fetch('https://api.github.com/repos/0xdps/emailflare')
        .then(function(r) { return r.json(); })
        .then(function(d) { if (d.stargazers_count) animateStars(d.stargazers_count); })
        .catch(function() {});
    }
    document.addEventListener('astro:page-load', runStarCount);
    document.addEventListener('DOMContentLoaded', runStarCount);
  </script>
</Base>
