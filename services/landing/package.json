{
  "name": "emailflare-landing",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "pnpm build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
